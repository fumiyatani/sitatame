org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
kotlin.code.style=official
# Wasm compilation (WasmIrToBinary) is memory-hungry; the default Kotlin daemon
# heap OOMs on the Compose Wasm target. Give it a larger heap.
kotlin.daemon.jvmargs=-Xmx4g
